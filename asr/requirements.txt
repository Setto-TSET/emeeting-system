typhoon-asr==0.1.1
fastapi==0.121.1
uvicorn==0.42.0
soundfile==0.14.0
numpy==2.5.2
# torch กับ nemo-toolkit มาทาง typhoon-asr ไม่ได้เรียกตรง ๆ แต่ต้องตรึงไว้ด้วย เพราะถ้าปล่อยลอย
# การ build อิมเมจใหม่ในอีกหลายเดือนจะได้เวอร์ชันคนละชุดกับที่ทดสอบไว้ แล้วอาการจะโผล่ตอนรันจริง
# (/health ตอบปกติ แต่ /transcribe พังทุกครั้งเพราะโหลดโมเดลไม่ได้) นี่คือชุดที่เทสต์ผ่านจริง
torch==2.13.0
nemo-toolkit==3.0.0
